/*
    This file is create and managed by Tahir Iqbal
    ----------------------------------------------
       It can be use only for education purpose
*/

#include <stdio.h>
#include <stdlib.h>
#include <math.h>

void main(void)
{
    int num;

    printf("Enter the number =");
    scanf("%d",&num);

    printf("The square root of number %d is %.2f\n",num,sqrt(num));

    return 0;
}
